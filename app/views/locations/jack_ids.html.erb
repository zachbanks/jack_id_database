<div class="page-header">
	<h1>Jack IDs</h1>
</div>

<div class="symbols-legend">
	<span class="label label-info">Important</span><span class="has-notes"> * </span> denotes that room has associated notes.
</div>

<div id="search">
	<%= form_tag jack_ids_path, :method => :get, :class => 'form-search' do %>
	    <%= text_field_tag :query, params[:query], :class => 'search-query' %>
	    <%= submit_tag "Find", :name => nil, :class => 'btn' %>
	<% end %>
</div>

<div id="jack_ids">
  <%= render 'jack_ids' %>
</div>

<div id="paginator">
  <%=  paginate @locations %>
</div>