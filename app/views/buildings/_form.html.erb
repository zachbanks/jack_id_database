<%= simple_form_for @building, :html => { :class => 'form-horizontal' } do |f| %>
	<%= f.input :name %>
	<%= f.input :short_name %>
		<%= f.submit submit_button_text(@building, "Building"), :class => submit_button_css(@building) %>
<% end %>